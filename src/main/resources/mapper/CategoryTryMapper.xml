<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.how2java.tmall.mapper.CategoryTryMapper">
	    <insert id="add" parameterType="CategoryTry" >
	        insert into category_ ( name ) values (#{name})    
	    </insert>
	    
	    <delete id="delete" parameterType="CategoryTry" >
	        delete from category_ where id= #{id}   
	    </delete>
	    
	    <select id="get" parameterType="int" resultType="CategoryTry">
	        select * from   category_  where id= #{id}    
	    </select>

	    <update id="update" parameterType="CategoryTry" >
	        update category_ set name=#{name} where id=#{id}    
	    </update>
	    <select id="list" resultType="CategoryTry">
	        select * from   category_      
	    </select>
	</mapper>

